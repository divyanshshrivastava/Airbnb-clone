<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit your Listing</title>
    <link rel="stylesheet" href="/edit.css" />
  </head>
  <body>
    <div class="edit-container">
      <h1>Edit Your Listing</h1>
      <!-- Using method override (_method=PUT) -->
      <form
        action="/listings/<%= cardInfo._id %>?_method=PUT"
        method="POST"
        enctype="multipart/form-data"
      >
        <div class="form-group">
          <label for="title">Title</label>
          <input
            type="text"
            id="title"
            name="title"
            value="<%= cardInfo.title %>"
            required
          />
        </div>

        <div class="form-group">
          <label for="description">Description</label>
          <textarea id="description" name="description" required>
<%= cardInfo.description %></textarea
          >
        </div>

        <div class="form-group">
          <label for="image">Image (Upload new if you want to replace)</label>
          <input type="file" id="image" name="image" />
        </div>

        <div class="form-group">
          <label for="price">Price (₹)</label>
          <input
            type="number"
            id="price"
            name="price"
            value="<%= cardInfo.price %>"
            required
          />
        </div>

        <div class="form-group">
          <label for="location">Location</label>
          <input
            type="text"
            id="location"
            name="location"
            value="<%= cardInfo.location %>"
            required
          />
        </div>

        <div class="form-group">
          <label for="country">Country</label>
          <input
            type="text"
            id="country"
            name="country"
            value="<%= cardInfo.country %>"
            required
          />
        </div>

        <div class="form-actions">
          <a href="/listings/<%= cardInfo._id %>" class="cancel-btn">Cancel</a>
          <button type="submit" class="save-btn">Save Changes</button>
        </div>
      </form>
    </div>
  </body>
</html>
